@PACKAGE_INIT@

ENABLE_LANGUAGE(C)

include(CMakeFindDependencyMacro)

set(OTHER_INC_DIRS)

if(NOT HDF5_FOUND)
  find_dependency(HDF5 REQUIRED COMPONENTS C CXX HL)
  # include_directories(${HDF5_INCLUDE_DIRS})
endif(NOT HDF5_FOUND)

foreach(target @TARGETS_EXPORT_NAMES@)
    include("${CMAKE_CURRENT_LIST_DIR}/${target}.cmake")
    message("Including target ${target}")
endforeach()

set(@PROJECT_NAME@_LIBRARY
      @LIBS_EXPORT_ALIASES@
)
set(@PROJECT_NAME@_LIBRARIES ${@PROJECT_NAME@_LIBRARY})

set(@PROJECT_NAME@_INCLUDE_DIR ${CMAKE_CURRENT_LIST_DIR}/../../../include)

set(@PROJECT_NAME@_FOUND TRUE)

check_required_components("@PROJECT_NAME@")

